Show a gif in UI that is based on a timer. If the timer is equal and less than 15 seconds then show the gif else display a message like running time has been over. How will you solve this problem statement? 

personing running.gif

 

Hint 1:- Use futur.delayed for the duration

Hint 2:- Use a flag based on time and add logic to rendering the gif and text.
Source Code:
import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Timer-based GIF Display',
      home: RunningGifScreen(),
      debugShowCheckedModeBanner: false,
    );
  }
}

class RunningGifScreen extends StatefulWidget {
  @override
  State<RunningGifScreen> createState() => _RunningGifScreenState();
}

class _RunningGifScreenState extends State<RunningGifScreen> {
  bool showGif = true; // Hint 2: Flag for showing gif

  @override
  void initState() {
    super.initState();

    // Hint 1: Use Future.delayed for duration
    Future.delayed(Duration(seconds: 15), () {
      setState(() {
        showGif = false; // Hide gif after 15 seconds
      });
    });
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Person Running Timer Demo'),
        backgroundColor: Colors.blueGrey,
      ),
      body: Center(
        child: showGif
            ? Column(
                mainAxisAlignment: MainAxisAlignment.center,
                children: [
                  Text(
                    'Running... ⏱️',
                    style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold),
                  ),
                  SizedBox(height: 20),
                  // Display GIF from assets
                  Image.asset(
                    'assets/running.gif',
                    width: 250,
                    height: 250,
                    fit: BoxFit.cover,
                  ),
                ],
              )
            : Text(
                '⏰ Running time has been over!',
                style: TextStyle(
                  fontSize: 22,
                  color: Colors.redAccent,
                  fontWeight: FontWeight.bold,
                ),
              ),
      ),
    );
  }
}
